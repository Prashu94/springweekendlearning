server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eazyschool
    username: prashant
    password: admin123@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      javax:
        persistence:
          validation:
            mode: none